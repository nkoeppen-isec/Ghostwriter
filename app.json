{
  "name": "GhostWriter",
  "description": "A project management and reporting engine by SpecterOps.",
  "repository": "https://github.com/yourusername/Ghostwriter",
  "addons": ["heroku-postgresql:hobby-dev"],
  "env": {
    "DJANGO_SECRET_KEY": {
      "generator": "secret"
    },
    "DATABASE_URL": {
      "required": true
    }
  },
  "scripts": {
    "postdeploy": "python manage.py migrate"
  }
}

